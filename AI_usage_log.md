AI Usage:

AI was used to generate python code:
1. To install libraries and download a pre-trained language model.
2.  To setp up the environment and load the model.
3.  To implement the functions get_ocr_text(image) and hybrid_vqa(image, question).
4.  To download a VizWiz subset from Hugging Face and save it to a csv file for future fine tuning.
5.  To load a small VizWiz dataset and test the VQA code with ten random samples.
